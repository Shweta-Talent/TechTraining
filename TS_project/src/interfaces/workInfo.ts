export interface IWorkInfo{
    workInfoId: string,
      fromDate?:Date,
      toDate?: Date,
      listOfIndividualDate?: Date,
      showWorkToTalent?: boolean,
      wordLocation: string,
      wordrequirements:string,
}